# TrafficLightSimulation
This Java program simulates a traffic light system using Swing GUI components. The program allows the user to select Red, Yellow, or Green radio buttons, and displays corresponding messages such as STOP, READY, and GO in the selected color.
